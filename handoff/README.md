> **Start with `CURRENT.md`** — one page: what ships, what is default, what is
> built but unproven, and the next gate. This README is the long-form index.

# CallistoSSS — handoff for external analysis

**Current status: shipping. AgX works; the hair BRDF is NOT visually
confirmed.** Confirmed on screen: the SSS kernel, the hair shadow-leak fix, the
skin BRDF, and the AgX tonemapper in **both display modes** — HDR 2026-08-27,
SDR 2026-08-28 — across all ten LUT-generator permutations, now running over
the game's authored per-area grading LUTs (`21-AGX-GRADE-AND-SDR.md`).
The hair anisotropy / dual-lobe package is confirmed only *statically*
(spirv-val, site counts, dead-id analysis) and confirmed to be **loaded** (70/70
resolve swaps, 2026-08-26); it has never been shown to change a pixel. Earlier
"confirmed" screenshots were contaminated: ray-bounce onto hair from Ultra Plus,
whose hair settings were not isolated, was read as this mod's effect. See
`09-SETTINGS-AUDIT.md` D11. The settings switches are **not** reliably
independently toggleable from the CET tab — see `09-SETTINGS-AUDIT.md` for what
each switch actually does and the plan to fix it.
The hair **shadow-leak fix** works and is confirmed on screen, shipped as its
narrowed `full-shadow` build (direct shadow rays only), which retains a
residual LOD-transition flicker on flat props — that axis is exhausted
(`26` §7b-d). Tier 1 of the PT work is on screen and clean: the reported hair
regression was **retracted** (`26` §4.3). Tier 2's first item, **diffuse
metal energy restoration (MS-GGX), was confirmed on screen 2026-08-28** — a
single-variable A/B, one launch with the splice on against four without — and
now ships default-on (`28`).
**`19-STATUS.md` is the ledger** of what works, what was built but never run,
and what was withdrawn. **`26-SESSION-0828.md` is the current resume point**
for the shadow track; **`27-SKIN-SPECULAR.md` §4 records the skin-panel A/B
verdict (no gloss from CVars; Tier-3 splice is the next build)**;
**`28-MS-GGX-ENERGY.md` records the metal-energy confirmation and the
default-on flip.
**Read `00-ARCHITECTURE.md` first** — it consolidates everything below, several
of whose conclusions it supersedes. Then read `10-DISPATCH-TRUTH.md`, which
corrects `00`'s coverage claims: they counted module *creation*, not dispatch,
and `15-RENDER-GRAPH.md`, which names the lighting buffers and their writers.
`16-ENGINE-HAIR-BRDF.md` then reframes the hair work entirely: the renderer
already exposes a three-lobe hair BRDF as engine CVars.

Read in this order:

| file | what it covers |
|---|---|
| `00-ARCHITECTURE.md` | **START HERE** — what the mod is, how it works, current state, open items |
| `47-PROCESS-TRACE.md` | **the decision record for the 2026-08-30 L1-L8 session — read with `46` §11-§18.** `46` is what was measured; `47` is why each launch was chosen, what was predicted before it ran, and where the reasoning went wrong. Written for a peer reviewer and deliberately preserving overturned reasoning in sequence. Two conclusions were withdrawn mid-session and one queue decision reversed, both from the same cause: **trusting an unreplicated baseline**. §2 the false 58% noise floor and the wrong RR mechanism it spawned; §3 the static pre-flight that corrected §9 c2 without spending a launch, plus the eyes call the user overturned; §4 the regime break at ~13:30 that invalidated every E1-baselined figure in §5; §5 why the RR arm was abandoned for a vanilla baseline; §6 the L5-L8 elimination chain, in which **two of three pre-registered predictions failed** (`ptclamp` and `ptreg` both cleared) before `ptbounce` was found to span the whole gap; §7 what stands and what died, with the measured per-region floors (S3 non-skin 0.3%, S1 skin ~6%, S3 skin ~9% - region choice matters more than metric choice); §8 self-critique, six errors ranked; **§9 the eight places a reviewer should attack first**, led by the uncharacterised regime break and the fact that RR state during capture is inferred, never observed. §10 reproduces every figure. |
| `46-AB-LEDGER.md` | **the A/B record — 14 launches; §1-§10 peer-reviewed, then largely overturned by §11-§18. Read `47` alongside it for why.** E0 bit-exact baseline (verified zero swaps), E1 shipping default, E2 both roughness directions; every figure regenerable by `a-b-testing/reproduce.sh`, every launch audited from the layer journal. Findings: the roughness axis works on screen and is a *texture* effect (+35/+48% fine energy, seen unprompted — "like a detail filter"); the `42` bounce-light fix shows **no runtime signature** on bounce-lit skin (reopened; one probe-cls launch decides between three causes); the shipping stack *loses* 16% fine skin texture vs vanilla (suspect: the firefly clamp, untested). §4.3 documents a genuine statistical trap — baseline-binned tone deltas are regression to the mean — that killed one wrong conclusion before it shipped. §9 = peer review: three corrections applied in place, mask-sensitivity and magnitude checks added, every number independently re-run and matched. §9.4 = the next four launches (A-B-A floor → probe-cls → ptclamp-off → RR-off). §9.5 = the fork recommendation (`rough-1.3` if forced). §10 = resume state for a fresh session. **§11-§18 are the eight-launch session of 2026-08-30 afternoon and they retire most of the above**: §11 (partly withdrawn) the first null relaunch; §12 a static find - `ab0bc2fe` writes an integer sample-index buffer, not colour, so §9 c2's exclusion fails and there is **one** colour-writing resolver, not two; §13 the **regime break** at ~13:30 that contaminates every E1-baselined figure in §5 and §6.2, caught only by a non-skin control; §14 the first valid mod-vs-vanilla measurement (the skin BRDF brightens the **lit half of the face only**, switching on at ~106 lum, independently reproducing the ~116 paint threshold §12/L2 measured with a different instrument - and it costs **no** S1 skin texture, so §6.2 is dead); §15/§16 `ptclamp` and `ptreg` both cleared by pre-registered predictions that failed, and all S3-**skin** figures withdrawn on a 9.3% same-config floor; §17 vanilla repeated so both sides are replicated - **-9.9%, 7.3x the within-cluster spread, non-overlapping**; §18 `ptbounce` spans the entire gap, and the sign flips: it is **GI convergence, not detail loss**, so it stays on. Also §18.3: the journal's `payload=` is a `stat` hash, not a content hash, and it collides. |
| `45-AB-PROTOCOL.md` | **the launch script — the launch script for the next model.** Pre-flight `cmp`s, the three files that prove what a launch served, the scene set (direct sun / bounce-lit / grazing), and eleven experiments in dependency order: bit-exact baseline → shipping default (= the `42` launch) → roughness direction → coupling → micro-shadowing → eyes → the combined `real` rung → kernel presets → sun size (live) → SER frame time → the RR/NRD control → decoding the probe. A decision table says what each null means. |
| `44-LOW-HANGING-FRUIT.md` | **built, deployed, nothing on screen.** Every low-hanging item from `38`/`43` as an optional feature: four skin realism axes (`alpha_scale`, `eye_alpha_max`, `dcouple`, `micro_k`) in the compute patcher, composable in one build, identity by default (the five old rungs rebuild **byte-identical**), parked as a **14-rung** ladder with per-rung coverage asserted; sun CVars; kernel presets; a SER selector. The roast: the layer had been **serving vanilla raygens on every `ser=class` launch** because vkd3d-proton enables the extension itself and the layer read its own no-op as "SER off", then rejected to vanilla instead of the next overlay (§2.1); CET could not even write `ser=` (§2.2); the game ran `sync_settings.sh` two commits stale with no deploy target (§2.3); `38`'s `+2` channel is the **shading normal**, so B1/M7 have no input (§2.4); `--sets --set` never overrode anything (§2.5); micro-shadowing structurally cannot reach 23 of 173 sites and says so (§2.8); the probe screenshot read as "G-U4 opens" (§2.9). |
| `42-BOUNCE-LIGHT-GATE.md` | **newest — a coverage bug, fixed; nothing on screen yet.** *"The oily skin effect only applies to skin in direct sunlight."* Confirmed, and the cause is bigger than the gloss: in the two indirect/GI resolvers — including `99bb7c2698997b2a`, the one module `10` proved dispatches **directly** — the class-1 gate reached **none** of the 218 lines the three passes splice at. Not the gloss, not the roughness cap, and **not the shipping tier-1 `c1`** either. Bounce-lit skin has never carried any part of this mod's BRDF, in either the hair-patcher era or the skin one. **Cause:** dxil-spirv guards the material fetch behind a bounds test and merges the class out with `OpPhi %uint %uint_0 … %330 …`; the `>>5` the patcher anchors on is defined *inside* the guarded block, so it dominates nothing below the merge, while the phi dominates everything — and the shader's own class tests read the **phi** (`%5297 = OpIEqual %bool %329 %uint_1`). **Fix:** `lift_class_gate()` scores the anchor against every line the three passes would splice at and, only on a strict improvement, moves it onto that phi (emitting no instructions — the value is already there and is the module's own); every module whose own shift already dominates keeps its old anchor byte-for-byte. **Why nothing caught it:** `mod.const()` emits the knob `OpConstant`s before any site is examined, so a pass that splices *nothing* still changes the file — the `off` → `medium` delta on `99bb` was **48 bytes** of dead constants, and `27` §8.3's "all 77 get the gloss" was read off exactly that. `spirv-val`, the per-rung byte assertions and the coverage-list check all passed. The build now **aborts** on any non-empty `skipped_dom`. **Verified:** 77/77 patched and `spirv-val` clean on all five rungs; coverage 157 → 173 c1 sites, 879 → 1071 gloss channels, 343 → 408 alpha caps, **0 skipped anywhere**; exactly **2 modules changed** vs the previous install in every rung, the other 75 byte-identical. **Tuning consequence:** `33` §2's roughness-flattening warning now applies to bounce-lit skin too, which promotes `33` §5's roughness *scale* from nicety to next thing to build. Per `GOTCHAS`, a spliced site proves an instruction exists, not that a pixel moved. |
| `41-SER-BUILD.md` | **built and validated offline; unproven until a launch.** Cyberpunk asks for Shader Execution Reordering (`cvRayTracingEnableReferenceSER`) and, under vkd3d-proton, **never gets it**: 0 of 3273 dumped modules declare `SPV_NV_shader_invocation_reorder`, while the driver reports `REORDER_MODE_REORDER_EXT`. This puts it back — `OpReorderThreadWithHintNV` spliced into **all twelve** `rgs_reference_main` permutations, four hint variants (`class`, `byte`, `hit`, `class+hit`) shipped as a **ladder** because neither candidate site dominates on paper: the class gate guards **62 of 14901 lines (0.4%)** and sits *inside* the sky test, while the bounce hit/miss gates **81.7%** but carries live state across the reorder. 48 modules `spirv-val`-clean at vk1.3 **and** vk1.4, read back with `spirv-dis`, +60 bytes on the `class` rung. `swap_layer.c` now intercepts `vkCreateDevice` to enable `VK_NV_ray_tracing_invocation_reorder` and chain its feature struct, since a swapped module declaring the capability is otherwise illegal. **Two findings the brief did not anticipate.** (1) The layer's capability constant was **5383, not 5345** — with the wrong value the reject guard was silently dead and would have served SER modules to a device without the extension; no offline check could catch it, only `./dev/patch_ser.sh --selftest` against the real driver (11/11). (2) **This driver does not reject SER modules when the extension is disabled** — `vkCreateShaderModule` and `vkCreateRayTracingPipelinesKHR` both return `VK_SUCCESS`, measured three ways. The enable stays correct per spec but is belt-and-braces, and the operational consequence is nasty: **an un-updated layer looks exactly like success** — game starts, 12 HITs logged, nothing reordered. **The standing risk** is a stale `swaps.ser/`: it must lead `CALLISTO_OVERLAYS` to outrank `swaps.ptq/`, which is only safe because it was built *from* it, so any ptq rebuild without a matching SER rebuild silently drops the tier-1/MS-GGX/skinray splices with no error anywhere. Per `GOTCHAS`, a swap HIT is not execution: **only a frame-time delta proves a reorder happened.** |
| `40-SUBTYPE-PROBE.md` | **built and validated offline; nothing has been on screen.** The first launch experiment from `38`: the material sub-enum probe (gate **G-U4**) merged with the ungated cloth-sheen probe (idea **A2**), five swap rungs from one build script (`dev/patch_subtype_probe.{py,sh}`, which *import* `patch_compute_skin`'s class machinery rather than modifying it). **Set 1** paints `palette[word & 31]` into every radiance write in **76 of 84** anchored compute libs at **151 sites** (31 of them refetching the material word locally because the module's own fetch does not dominate), with a screenshot-decodable palette: 16 hues x 2 gains = 32 injective slots, the nine most-tested sub-enum values on the eight *cardinal* hues plus near-black, everything else on the in-between and dark rings — decode hue first, bright-vs-dark second, exact value by R:G:B ratio against the legend in §4. **Set 2** adds an **ungated** Charlie (Estevez & Kulla) sheen x Neubelt visibility at **464 of 481** GGX sites in **82** modules — five of which the class-gated skin overlay has never been able to reach — sized to be unmistakable (+15.9 at grazing, +0.006 head-on) and explicitly **not** Zeltner/Burley/Chiang's LTC form, whose fit table would need a descriptor (U1, not built). A `cls` rung byte-identical to `patch_compute_skin.py --tier hunt` is carried as the **positive control**, painting the *same 76 modules at the same 151 lines* as `sub`, so a null on `sub` is attributable to one `OpBitwiseAnd`. **Corrects `38` §1.3's census:** under a rule requiring one word to feed both `>>5` and `&31`, 81 modules qualify (59 GLCompute, 12 Fragment, 10 RayGeneration), 67 test a value, the values are `{12,13,14,15,17,21,25,26,30,31}` — **`0` and `16` are not sub-enum values** (they are `&31` on a different word), there is a tenth value `26` in one Fragment module, and **compute only ever branches on `{17,21,25}`** while Fragment tests all ten. Class 2 remains tested by zero modules anywhere. Wiring rides the existing `skin` overlay as `skin.set/probe-<rung>/` levels because the overlay list is baked into `swap_layer.c` and env vars do not reach the game; `sync_settings.sh` wipes `swaps.skin/` first, so there is no first-file-wins collision, and the rungs deliberately carry **no c1 and no gloss**. Verified: 386/386 `spirv-val` clean at their own `spv1.3`, **no vanilla line deleted in any module** (modified lines = exactly 151 image writes for the paint rungs), the palette chain re-parsed out of the emitted SPIR-V and evaluated **14496** times with 0 mismatches, the sheen chain re-parsed and mirrored in Python to **1.7e-07** worst relative error. Known magnitude bug, harmless for a probe: 464 sheen sites map to 365 distinct `(NoH,NoL,NoV)` contexts, so **99 sites double-add**. §9 names the scene to capture (daylight exterior, companion NPC, two fabrics, one hard non-cloth reference, grazing silhouettes, framed like `pics/panam_working_small.png`) and §10 what every outcome means — including the most valuable one: a `sheen`-null while `cls` paints would rule out **every future BRDF edit at the specular site in the compute evaluators**, gated or not. Per `GOTCHAS`, a swap HIT is not execution. |
| `39-TRANSLUCENCY-REMOVED.md` | **a removal record and a postmortem.** Tier-4 backlit skin transmission (`skintrans`) and its anatomical-thickness variant (`skinthick`) are **gone from this repo** — the SPIR-V pass, the ladder tables, the CET switches, the `sync_settings.sh` keys, 56 build/parked sets (661 MB), and docs `30`/`34`/`35`. It was built, seen on screen and wrong: *"the edges of geometry cast a blocky red glow straight through the face"*, at `medium` and not only at the `extreme` diagnostic. **The verdict is that the method was wrong, not the tuning**, and §3 names four mistakes. (1) **The term has no per-pixel shaping and that was knowable before the build** — `V` and `L` are near-constant over a face, so `back` is a *wrap*, not a rim, and `saturate(-N.L)` is ON across the whole front of a backlit head: a forehead scores as high as an ear. `29` A4 predicted this verbatim, and the route was taken anyway because it ranked *cheapest*. (2) **Thickness was never available, and three proxies in a row did not make it so** — `1-sunShadow`, `CharacterLightBlockers`, `pow(1-|N.V|, t_rim)` and depth-ring openness, each added after the last was seen to fail, each a smaller correction to a base shape already wrong. The user's objection — ~15 knobs approximating one physical quantity is a symptom — was recorded as a finding and then not acted on. (3) **The splice site was wrong for a term that ADDS light**: every earlier tier *multiplies* into `15` §2's already-quantised 8px signal, so the tile grid is invisible; a tier that adds draws that grid as a step edge, and no knob in the compute resolvers reaches it. (4) **The verification proved the build, never the picture** — 77/77 `spirv-val`, per-axis byte assertions, byte-identical `off`, hand-read SPIR-V, a full sibling sweep: every check answers "did the instructions I intended get emitted", **none** could answer "does a forehead glow". That gap is `GOTCHAS`' *a structural detector proves a site's shape, never what it means*, and this is its largest violation in the repo — not by ignoring the rule but by satisfying it in letter while leaving the actual question unasked. §5 lists what was deliberately **kept**: `GOTCHAS` 12 and 13 (both paid for by this work and both general), `29` Part B, the engine's own `CharacterSubsurfaceTranslucency` CVar, and the Tier-3 gloss ladder. §6 gives the two conditions under which restarting would be honest — a real thickness signal (needs an engine-side binding, not a heap index) **and** a different splice site (the RT path, whose output is not tile-quantised) — both currently unmet, so the correct amount of this feature in the repo is none. |
| `38-WILD-IDEAS.md` | **investigation only, nothing built or launched; one splice validated offline.** A brainstorm against the 2024-2026 literature that turned into four measurements, three of which lower walls this project has been building around. **(1) The game gets no Shader Execution Reordering here:** 0 of 3273 dumped modules declare `SPV_*_shader_invocation_reorder`, while the exe ships `cvRayTracingEnableReferenceSER` and this driver reports `REORDER_MODE_REORDER_EXT` — Cyberpunk uses the DXR HitObject path on Windows and vkd3d-proton does not translate NVAPI shader intrinsics. Putting it back is **3 instructions / +60 bytes**, `spirv-val` clean at vk1.3 *and* vk1.4, keyed on the class the raygen already fetches; it cannot change a pixel, so frame time is the only honest proof. **(2) The material byte splits twice:** `word >> 5` is the 3-bit class everyone knows, and `word & 31` is a **populated 5-bit sub-enum** tested in 68 modules with values `{0,16,17,21,25,30,31}` (fragment `667c…` adds `{12,13,14,15}` and routes seven of them to one arm that picks per-subtype constants out of a CBV). `4d46` fetches the byte and consumes only `>>5` — the subtype costs **one `OpBitwiseAnd`**, and it is the cloth gate `22` §5 called unreadable offline. **(3) The G-buffer table has a hole and two direction-shaped channels:** `registers[1]+3` is R8_UINT 1280x720 and is skipped by 9 of 9 family-A evaluators and both GI resolvers — addressed *relative to a push-constant base*, so GOTCHAS 13 does not bite — though 105 of 220 other compute modules use that offset off the same base, so it is **not yet** a free channel; and `+1`/`+2` are both 10:10:10:**2** with `4d46` decoding `+2` as a normalised unit vector and scaling **both** 2-bit alphas by 3, which is the shape `22` §2's unchased octahedral finding needs, corroborated by `EMM_SurfaceHairDirection`/`HairID`/`Translucency`/`ObjectID` and by `GBuffer0A`/`GBuffer1A` being separate debug views. **(4) BDA is universal** — 3225 of 3273 modules, not just Fragment as `36` §4b measured. Answers the four questions asked: tensor cores **are** in use under Proton (DLSS/RR/FG) and the layer sits *below* vkd3d-proton where CoopVec rev 4 is live, so this mod can reach them where the game cannot; there is **no** lossless GPU texture format and Kraken already has the disk axis, so `36` G0 remains the only honest next step; and faces cannot get sharper inside a compute resolver because lighting is 720p, tile-classified (measured: a **40x23 R32_UINT** list, i.e. 32x32 tiles) and then upscaled *and* velocity-smeared — which makes the never-patched **Fragment stage** (`36` G1, still unrun) the highest-leverage experiment in the repo. Organised unlocks-first: U1 descriptor injection via RED4ext, U2 the fragment stage, U3 the skipped slot, U4 the sub-enum (needs nothing built), U5 the standing payload sentinel. Then 21 ideas in four tiers, each with its mechanism, its paper and its falsifier — SER, LTC sheen, vellus sheen, specular AA from normal variance, biophysical SSS kernel, pre-integrated curvature, thin-film cyberware, a geometric hair tangent, glints, real refraction, and a class-gated **warp-uniform** CoopVec network that structurally dodges the divergence that killed `36`. Recommended order: G-U4, then G-A1, then G-U2 — and G-U4 merges with the sheen probe into **one launch** that answers three questions at once. |
| `37-SAMPLING-DECORRELATION.md` | **feasibility only, verdict negative, nothing built.** Can Cranley-Patterson / Heitz-Belcour per-bounce rotation ride the LCG that `29` B4 found, for zero new resources, now that `24` §4 killed the blue-noise LUT? **No, on three independent measured findings.** (1) **The bounces are already decorrelated** — the very phi the idea cites as its enabler is what does it: `%704` advances 3-10 LCG steps per iteration and bounce 1 starts where bounce 0 stopped, so measured bounce-to-bounce correlation is **0.0033 worst case against a 0.00104 noise floor**, across all plausible advance counts. There is nothing to decorrelate. (2) **Cranley-Patterson is a provable no-op here** — it randomizes a *low-discrepancy point set*, and there is no Sobol/Halton/Owen/golden sequence in any of the 12 permutations (checked); on a plain LCG `frac(u+c)` is a measure-preserving bijection, and mean/variance/correlation are unchanged to three digits for every `c` tested. (3) **The blue-noise half is what had the value, and it is what `24` §4 killed** — measured error power spectra put the shipped sampler at HF/LF 1.02 and the proposed edit at 1.05 (both white), while a real blue mask reaches **23.3**. The citation is inverted: the blue-noise-ness comes from the *mask*, not the rotation. Worse, a **third** requirement not previously identified anywhere: even *with* a mask, bolting it onto this sampler still gives white error (HF/LF 0.97), because the per-pixel seed hash re-randomizes every pixel and destroys the mask's spatial structure — getting the gain means **deleting** the seed at `:1395-1429` across 12 permutations, not offsetting downstream of it. Also checked and also negative: the low-24-bit mask (`s & 0xFFFFFF`) is the classic weak half of an LCG and bits 0-3 do have periods 2/4/8/16, but consecutive-pair chi2/dof sits at 1.0 for both it and `s >> 8`, so **no change is warranted** — recorded so it is not re-derived. Leaves `32` §4's ladder exactly where it was: the sample loop **does not need** CP rotation (B4 already gets decorrelation free from the phi, now confirmed empirically) and **gains nothing** from it. Ships `dev/validate_sampler_rng.py`, a bit-exact model of the seed hash and LCG, so every number above is one command. New GOTCHAS rule proposed: *a named technique from a real paper can still be a no-op here — check which of its preconditions this renderer actually meets before importing it.* |
| `36-NTC-FEASIBILITY.md` | **investigation only, nothing built or launched.** Can NVIDIA's RTX Neural Texture Compression put HD Reworked (276 files, **970.2 MB** uncompressed, parsed from its RDAR index) back in the game without the VRAM? **The plumbing came out in favour and the throughput did not.** Four findings all land positive: every Fragment shader that samples uses one idiom (**239/239** measured) that computes the absolute bindless slot in-shader, so a structural detector is writable *and* the shader already carries a per-material identity; fragment modules already declare `PhysicalStorageBufferAddresses`, so weights, constants and a slot->set table reach the shader from a raw device address baked in at `vkCreateShaderModule` time — **no descriptor set, no pipeline-layout surgery, no touching vkd3d's bindless heap**; a *live* slot->set table answers GOTCHAS 13 (nothing is baked, so nothing has to be stable); and the PT path is not blocked by the missing derivatives, because `chs_main_*` already uses `OpImageSampleExplicitLod` exclusively and hands NTC the `mipLevel` it wants. CoopVec is present (`VK_NV_cooperative_vector` rev 4, driver 610) and Proton puts us on NTC's **shippable** Vulkan branch rather than its do-not-ship DX12 one. What kills it is §6: the paper's own Table 4 is **2.3-3.9x BC7 on a 4090 for one texture set with zero divergence**, software trilinear is an 8x slowdown (so STF is mandatory and hardware aniso is gone), and §5.2.1 says multi-material SIMD divergence — which *is* Cyberpunk's G-buffer pass — was never evaluated and is left as future work. Second killer, independent of perf: NTC's ratio comes from bundling correlated channels, HD Reworked replaces mostly `_d`/`_m` and leaves the `_n`/`_r` siblings vanilla in the 61 GB base archives, so real bundling **expands scope from 276 textures to every material that touches one**. Inference-on-**load** is ruled out on NVIDIA's own table — it saves disk and PCIe and **zero VRAM**. Inference-on-feedback is the right shape and lives inside the streamer where no layer can reach. Ends on a gate ladder where four cheap diagnostics (G0 measure, G1 prove a **Fragment**-stage splice executes at all — never yet done here, GOTCHAS' dead `OpTraceRayKHR` is the precedent — G2 prove BDA injection, G3 prove the math runs on DP4a before CoopVec, G4 prove the descriptor write stream is observable) can close it before a feature exists. **G0 is half an hour and unblocks everything: nobody has measured what HD Reworked actually costs in VRAM, and the engine already ships `PoolGPUTextures`, `m_textureMaxMipBias` and `TextureQuality` as the free control NTC has to beat.** |
| `33-FACIAL-DETAIL.md` | **two shipped defaults were the cause, both now fixed.** "Faces read soft, I need them to look like real people; the bounce lighting is very smoothed over." Corrects the premise first: **path samples cannot produce pore microshadows** — pores are not in the BVH, only in the normal/roughness maps, so more samples cut noise and create no occlusion. The softness was downstream, and two-thirds of it was self-inflicted. **(1)** `author_callisto_kernel.py` carried `OFFSET_SCALE = 10.0` — the SSS diffusion blur, a spatial blur over skin's diffuse lighting, was running at **ten times the engine's own radius** (measured: tap offsets 0.00007 → 0.00073), with `CENTER_SOFTEN=0.35` moving another 35% of each pixel's own light out to its neighbours. Now a `--preset` ladder; **`detail`** (engine radius, no centre soften, red tail kept) is the new default and is installed, with `vanilla` verified byte-identical to the engine's own upload as a true control and the old shape parked as `callisto`. **(2)** `skinspec`'s `alpha_max` is a roughness **ceiling**, and against authored skin (roughness 0.40–0.60 = alpha 0.16–0.36) every rung clamps **every skin pixel to one constant alpha** — the served `medium` gave 0.09 flat, erasing all the roughness variation where skin micro-detail reads. Default flipped to `off` in **both** files that hold it. The real fix is a roughness *scale* rather than a cap, scoped but not built. **(3)** What is left is the denoiser, and the exe exposes all of it — `Editor/Denoising/{NRD,ReBLUR,ReLAX}`, `Editor/SHARC`, DLSS sharpness — none of it previously known here. Built as `detail_engine.lua`: 22 live knobs plus a one-click **"Sharpest possible"** diagnostic. Names SHARC as the literal answer to "bounce lighting is smoothed" (a world-space hash cache; cell size caps indirect detail regardless of ray count), and carries the caveat that DLSS Ray Reconstruction **replaces NRD**, so those knobs may be bypassed rather than broken. Also: sharpening is not the fix (it raises contrast on what survived, noise included), and **no PT internal-resolution CVar exists** — the only `ResolutionScale` in the exe is SpeedTree's. |
| `32-PT-SAMPLING-PANEL.md` | **built, nothing observed.** "More path samples where it counts". Corrects the premise first: **there is no per-material sampling in this engine today** — `RayNumber` is a global spp budget, `AdaptiveSampling` spends by screen-space **tile variance** (so a face in a shadow gradient attracts rays as a side effect, but it cannot be told "faces"), and `CharacterSubsurfaceStochastic` is the SSS blur's sampling, not the integrator's. What was built is `pt_engine.lua`, the engine-first panel (`29`'s ranked item 2, carried unrun): 12 RT sampling CVars, live, master off, own persistence file. Its one design problem is that the exe **deduplicates CVar keys**, so which group owns `RayNumber` cannot be read from the string table — the same inference that `22` §3 and `27` §2 made oppositely, one of them wrong. So each knob carries a **list of candidate paths**, resolves at register time to whichever answers, prints the winner into its own description, and a wrong guess degrades to one knob that says it is dead rather than to `26` §5's silently inert slider. 35 stubbed checks. It exists to settle Ultra Plus's `0xDEADBEEF` verdict on `RayNumber`/`BounceNumber`, which is a claim, not a finding — `29` §B3 shows the bounce bound is a runtime cbv value in 8 of 12 permutations. The **per-material** version is `29` §B4 and is not started: it wires the degenerate outer loop `%12276`/`%12818` (a sample-loop skeleton whose continue block nothing branches to, with the LCG state already a loop-carried phi so samples decorrelate free), and it is **gated on a sentinel launch** — GOTCHAS' "a second `OpTraceRayKHR` does not execute" is argued narrower than it reads (`29` §B5) but is not disproven. Also restates the ceiling: extra samples cut **noise**, not softness — PT is 720p internal reconstructed to 1440p by a filter with no material awareness. |
| `31-WET-EYES.md` | **a plan, nothing built.** Wetter eyes. GOTCHAS 8 asked and answered offline: the engine's entire eye-shading surface is one boolean (`Editor/Characters/Eyes` → `UseAOOnEyes`), and there is no `cvEye*` constant, no `cornea`/`sclera`/`iris`/`tearfilm` string anywhere in the exe — the cloth situation (`22`), not the hair one (`16`). The gate is free and already proven on screen: eyes are material **class 8**, painted violet by the hunt net in `pics/panam_working_small.png`, and `acquire_class_shift()` recovers the class value in all four G-buffer idioms, so class 8 costs one `OpIEqual` even though no shipping module tests it (`20` §1: only {0,1,3,4,5}). The effect is the **GGX roughness ceiling** already written for the gloss — F0 is deliberately NOT raised, a tear film is F0≈0.02, *below* the 0.04 default, so wetness is smoothness and an F0 raise would make chrome marbles. The load-bearing constraint is §4.1: `build_skin_alpha_cap` ends in `replace_all_uses`, so a second eye-gated call does not stack — the first call's rewrite is still pending in `edits`, the second finds nothing to rewrite, and its select is dead with `spirv-val` clean (the `08` dead-sheen bug + GOTCHAS 12). One pass, nested selects, one rewrite per alpha id. Phased so the matrix is not paid for first: the 5×5 ladder is already 26 sets / 166 MB / ~31 min, and a fourth 5-rung axis would be 130 sets / ~830 MB / ~2.5 h — so Phase 2 builds **two** sets (eye off vs extreme) as the mechanism check, and the axis is only built after eyes are seen to move. |
| `29-FACE-TRANSLUCENCY-AND-RAYS.md` | The feasibility analysis. **Part A (translucency) was built, was wrong, and has been removed — its body is now a pointer to `39`.** Part B (rays) is untouched and still open. What survives from Part A and is still cited: the engine ships the whole raster-side translucency subsystem (`renderstage_skin_translucency`, `CRenderNode_RenderSkinBackDepthForTranslucency`, `EMM_SurfaceTranslucency`) and **none of it reaches the traced path** — the free live-CVar A/B in A3 (`CharacterLightBlockers` off) is still unrun; the evaluators read **seven** images and `heap19[pc1+0]` is the front depth; a previously unnoticed **dual-lobe skin specular** driven by an 8-entry per-character profile table, which the Tier-3 gloss lands on both lobes of; and A4 R3 — the engine's back-depth pass **is found** (depth-only 1280x720, uniquely `clear=1.0` for reverse-Z, 25 indexed draws) and **does run in Overdrive**, settling a standing `GOTCHAS 5` worry, but its bindless heap index moved 73203 → 503350 across two captures **29 seconds apart in one session**, which is now `GOTCHAS` 13. The header verdict is left standing with its error marked: *"the only missing input is thickness"* is the mistake in one clause — thickness was not missing, it was **unavailable at that site**, and a feature whose one required input cannot be obtained is not a go at any cost. Rays: the material class **is** already fetched and tested in `rgs_reference_main`, so a skin gate at ray level is free; the bounce bound is `cbv99[188].z` in 8 of 12 permutations and a baked `%uint_2` in the other 4; the real lever is the **degenerate outer loop** `%12276`/`%12818`, a sample-loop skeleton whose continue block has no predecessor, with the LCG state already a loop-carried phi so samples decorrelate for free. All of it gated on one sentinel launch. |
| `28-MS-GGX-ENERGY.md` | diffuse metal energy restoration (`23` T2.1): the MS-GGX energy compensation, shipped and **confirmed on screen 2026-08-28**. Rough metal renders dark because the path tracer's single-scatter GGX drops the light that bounces off a second microfacet; the splice multiplies the specular lobe by `comp = 1 + strength·F0·max(loss(α),0)`. The loss polynomial is **fit to this game's own lobe**, not a textbook's — the engine's sum-form Vis already recovers about half the loss by accident, so a Lazarov/Karis fit would roughly double-compensate — and it is normalized against the lobe's own α→0 limit (exactly 0.5, which makes any constant scale error cancel; that reframing is what killed the months-old `E_ss` blocker, which turned out to be two misreadings: the area-light arm read as the BRDF, plus a doubled NoL). α-only by design — the grazing-angle sum-vs-product error in the same Vis is larger but deliberately excluded. Both GGX arms (punctual + area/tube) patched in 10 of the 12 `rgs_reference_main` permutations; the other two are a monochrome-specular variant with no F0 and are skipped loudly. Confirmed by a single-variable A/B (one launch `m` on vs four off; journal lines plus swap-log size signatures, +2152 B on every patched module), then flipped to default-on. |
| `26-SESSION-0828.md` | **read with `25`** — session record for 2026-08-28, and the document that **closes the shadow-ray track**. The narrowing landed: `full-shadow` (flags `28 -> 12` on the 10 `rgs_shadow` modules only) closes the hairline seam with less flicker than the original `full`, and now **ships as the default**. Everything else was deleted. The reason it could go no further is §7d: the **two-ray splice does not execute** — `sctrl`, a positive control whose second ray was unculled with the same mask, *had* to reproduce `full-shadow` and came back vanilla — which retroactively voids the opacity split and all twelve cull-mask bisects. Along the way: the class-1 hole (shadow traces carry mask 118, GI 119), two falsified theories (sun-ray self-intersection, opacity as a discriminator), and two confounds that cost the session — an `m112` launch read as a `full` launch, and the PT tier-1 "regression" (later retracted, §4.3). Also three bugs: pipeline caches were being evicted **every** launch (`cp -f` stamping fresh mtimes into the payload hash — fixed), the six numeric skin-BRDF sliders are **inert** (found, not fixed), and `nativeSettings.removeSubcategory` corrupts tab key order (avoided). Carries the resume point. |
| `27-SKIN-SPECULAR.md` | skin specular sheen feasibility + **Phase 1 shipped**: the engine already parameterises skin's specular response (8 skin CVars verified in the exe — rim Fresnel/specular/const-offset trio, subsurface specular tint, ambient mix, feature gates), now exposed as a live CET panel (`skin_engine.lua`, 17 knobs, default off, vanilla-snapshot, 2 s re-assert). The mod-side half — Callisto **Tier 3**, the specular-Fresnel sheen term from the original plan, never built — is scoped as Phase 2 in the compute resolvers. **A/B done 2026-08-28: the CVars cannot gloss skin** (rim = N·V edge glow, tint = recolor, ambient = fill — nothing scales the GGX lobe's F0 or roughness), so **Phase 2 (the Tier-3 splice) is a go**; `27` §4 carries the finding and the plan. **Phase 2 is now built and wired (`27` §7)** — a skin-gated Fresnel reshape plus a GGX roughness ceiling, 284 groups / 852 channels, `spirv-val` clean, byte-exact when off, parked as an A/B pair so the `skinspec` switch moves the skin math against an identical hair BRDF. **Never launched.** **The hair BRDF was removed 2026-08-28 (`27` §8)** and the skin work now lives in `dev/patch_compute_skin.py` on its own `swaps.skin` overlay — coverage went up (77 modules vs 74), the shipped c1 is no longer gated behind a hair switch, and the gloss now reaches indirect-lit faces too. The hair shadow-leak fix and the engine CVar panel are kept. **Strength is a ladder, not a slider (`27` §9)** — the knobs are `OpConstant`s baked at build time, so `--sets` pre-builds five rungs (off / subtle / medium / **strong, the default** / extreme) that the CET selector picks between at launch; the roughness ceiling is the lever that matters, and the previously shipped 0.2025 capped at 0.45 against skin authored at 0.40-0.60, i.e. it barely bit. |
| `25-SHADOW-FLICKER.md` | **superseded by `26` §7b-d, closed in §10** — the shadow-leak fix's LOD-flicker regression on flat props, and the two builds tried against it. Both §8's opacity split and §9's cull-mask bisect are **void**: they varied a second ray that never executes. The standing conclusion is §10 — ray flags are per-*ray*, so no subset of trace sites separates the hair from the flat props, and the residual flicker is not reachable on this axis. Still carries its corrections table: §4's "no occluder-material signal at trace time" was false, §6's GI toggle would not have fixed it (36 direct shadow traces vs 2 GI), §3's coverage claim contradicts `17` §2. |
| `24-PT-TIER1.md` | tier 1 of `23`, built. Bounce-ray `cullMask` 1→255 (the hair lever, and the headline), a per-segment indirect firefly clamp, and path regularization — all three spliced into the twelve `rgs_reference_main` permutations, plus the cullMask edit on the three reflection raygens; four CET switches. **On screen, and it REGRESSED HAIR** (2026-08-28): with all four on, the hair shadow-leak fix no longer looks right; with all four off it does. Narrowed by A/B to one of the two cullMask wideners — `ptbounce` or `ptrefl` — since `ptreg`+`ptclamp` together are clean. **Retracted 2026-08-28 — there was no regression; all four ship clean (`26` §4.3).** See `26` §4.2 for the per-launch payload fingerprints that isolated it. Also: T1.3 (blue noise) **killed** by its own step (a) — the only 128×256 R16_UNORM upload in the survey is 58% exact zeros, so no noise LUT exists to author into. And the engine's own `cb[85].z` firefly clamp, found dead in this mode and a CVar candidate. |
| `23-PT-IMPROVEMENT-BRAINSTORM.md` | brainstorm of PT improvements Ultra Plus cannot cover (it is CVar-only), triaged by the three proven levers: path regularization + indirect clamp in the reference raygen, STBN noise-LUT swap, the combined class-weighted sheen-rainbow Phase 0 (merges `22`'s two probes into one launch), the ReSTIR mode's consequences for splice siting, and the promoted G-buffer hair-direction lead. Ideas only — nothing built. |
| `22-CLOTH-BRDF-FEASIBILITY.md` | cloth feasibility (investigation only). Proven: **no cloth BRDF exists** — clothing is Standard, shaded as renormalised Burley + one GGX lobe; zero cloth CVars. A Charlie sheen lobe is the cheapest BRDF addition ever scoped (~15 instr, all inputs live) but the cloth class ID is unknown and may not exist as a gate. Side-findings: a G-buffer hair-direction decode that contradicts `11` §2, and one executing module whose rejection needs a re-check. |
| `21-AGX-GRADE-AND-SDR.md` | AgX over the game's authored per-area grade, and the SDR splice that was in the wrong place. The area LUTs now survive the tone-curve replacement (`--set grade=1`, the default). `--site sdr` spliced *above* the engine's own tone curve, which then tone-mapped AgX's output a second time — re-derived as `--site sdr2`, anchored on the runtime gates bracketing the curve, which covers all four corners of the SDR 2×2 lattice. Colour space proven from the Stephen Hill ACES fit pair. Confirmed on screen in both modes. **Supersedes `18`'s SDR section.** |
| `20-GLASS-REFRACTION-FEASIBILITY.md` | glass/refraction feasibility (investigation only, nothing built). Proven from the binaries: **no refraction exists anywhere** — no Refract instruction, no IOR constant, no transmission lobe, no CVar, no render node. Glass = raster blend + screen Distortion + a traced *mirror* reflection (`rgs_reflection_transparent_main`, executes live). Dispersion/caustics triaged: dispersion reachable only behind an authored refracted ray; real caustics out of scope. Contains the scoped Phase-0 marker spec (not built). **Reviewed and corrected 2026-08-28** — the verdict stands, but three details under it were wrong (no glass Fresnel exists; the ray origin's sign is wrong for transmission; the planned `F·reflected + (1−F)·refracted` composite would ghost) and the engine's `RayTracing/Reflection` CVar group had been missed. See its corrections log. |
| `GOTCHAS.md` | **read before starting anything new** — the method rules and the mechanical traps, each one paid for by a wasted session |
| `19-STATUS.md` | **the ledger** — every feature with its state *and its evidence*; what is built but never run (tint net, hunt net, rim-three Phase 0); every claim withdrawn or falsified, with where it was corrected; the recurring "patched one of N siblings" failure mode and the three rules it produced. |
| `18-AGX-FEASIBILITY.md` | AgX tonemapper, **WORKING in HDR and SDR** — but read `21` after it, which supersedes its SDR section and its "current state". The tonemap LUT is GPU-generated; **TEN** generator permutations exist (2 HDR + 8 SDR) — `dev/find_lut_gens.py` finds them all, `dev/build_agx.sh` builds them, `dev/install_agx.sh` installs them. Two bugs are written up because both are general: the HDR splice wrote Rec.709 into a slot holding **CIE XYZ** (pink/cyan neutrals), and the SDR splice first patched **one encode branch of seventeen**. Presets: neutral / punchy70 / **punchy70desat** (installed) / punchy / golden; `mix` is an A/B knob, not a strength knob. |
| `17-LEVERS.md` | **newest** — where Callisto still has reach (ray-level edits + LUT authoring, neither CVar-reachable); trace-ray flag/cull-mask survey; the bounce-ray `cullMask=1` lever; negative result on the 256×8 LUT. |
| `16-ENGINE-HAIR-BRDF.md` | **newest — read before any more hair shader work** — the engine ships a live-tunable R/TT/TRT hair BRDF as CVars (verified in the exe); Ultra Plus only flips them. New CallistoSSS panel exposes all 40, applying live. |
| `15-RENDER-GRAPH.md` | **newest — START AFTER 00** — the compute render graph, from offline capture replay: 6ac9's real inputs/outputs, the four lighting families and who writes them, 22 named unpatched indirect-light candidates. Withdraws `14` §4; corrects `13` §3–§4. |
| `14-PROVENANCE.md` | provenance hunt: capA facts + dead ends. **§2.4 ("replay segfaults") is WRONG and §4's plan is withdrawn — see `15`.** |
| `13-OWNER-NAMED.md` | bisection named `6ac9085c9bd4b7da`: the temporal resolve owns hair's per-pixel pixels; architecture (tile evaluators → resolve); interior-hair evaluator is a non-class-gated module |
| `12-FRESH-HUNT.md` | Phase 0 probe ran but was out of scope (inconclusive); dispatch-driven fresh hunt: third class idiom found & patched, 29-module net staged |
| `11-PROPER-HAIR.md` | what a real strand tangent would take: the G-buffer has none and no free channel; three delivery routes, tooling needed, and the probe that gates the decision |
| `10-DISPATCH-TRUTH.md` | **READ WITH 00** — what actually dispatches: 16 of 70 patched modules, only the coarse GI resolver directly; the anchor scan selects the wrong family; 4 executing modules unpatched |
| `09-SETTINGS-AUDIT.md` | why the settings page can't be trusted: 11 confirmed defects, root causes, phased plan, invariants |
| `08-DUAL-LOBE.md` | shifted dual-lobe hair (R+TRT); two latent spec-output bugs found & fixed |
| `07-COMPUTE-RESOLVE.md` | the visible pixels are shaded in compute; RT passes only produce samples |
| `06-PT-IS-THE-CHS.md` | live PT shades in the closest-hit shader, not the raygen; explains every null result |
| `05-SHADOW-ANCHORS.md` | the shadow-raygen anchor family (built); why the reference anchors really failed to port |
| `04-RESET-STATE.md` | full saga, proven facts, open contradictions, post-reset checklist (fact 4 corrected by 05) |
| `01-BLOCKER.md` | the null-result problem, all evidence, what is ruled out (resolution note on top) |
| `02-PROJECT.md` | what this mod is, how the injection works, what shipped |
| `03-HAIR-WORK.md` | the hair BRDF work that hit this blocker |
| `evidence-raygen-permutations.md` | generated table: 12 raygen permutations, 2 patched |
| `callisto_swap.sample.jsonl` | the HIT lines + one line per distinct raygen permutation |

Everything referenced lives in the repo at `CallistoSSS/`, branch `dispatch-truth`.
Deeper background: `dev/HAIR_HANDOFF.md`, `dev/MS_GGX_NOTES.md`,
`../analysis/BRDF_HANDOFF.md`, `../analysis/HANDOFF.md`.

## One-paragraph summary

CP2077 runs under Proton, so every shader reaches the driver as SPIR-V. A
Vulkan layer intercepts `vkCreateShaderModule` and substitutes patched modules
keyed by the DXIL identity that dxil-spirv preserves. A skin-BRDF patch
shipped and is visibly correct in A/B screenshots. The hair-class hunt that
followed never rendered, and a long instrumented chase (dispatch logging in
the layer, full-permutation patching, capture-replay correlation) established:
the hunt build is correct and attaches to the right modules — proven by
replay — but every live session, with and without the Ultra Plus PT mod,
dispatched only `rgs_shadow_main`-family pipelines, so the patched reference
integrator never ran. After the reset, one clean PT launch with dispatch
logging decides between the three remaining hypotheses (PT silently not
engaging, a game-build change, or an instrumentation hole). See
`04-RESET-STATE.md`.
